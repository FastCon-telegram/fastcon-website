# FastCon Website 🌐

Современный лендинг для FastCon с админ-панелью, статистикой посещений и мониторингом серверов.

![FastCon](https://img.shields.io/badge/FastCon-Landing%20Page-blue)
![Node.js](https://img.shields.io/badge/Node.js-20+-green)
![Docker](https://img.shields.io/badge/Docker-Ready-blue)

## 🎯 Возможности

### Главная страница
- ✨ 3D глобус с названием внутри (вращается и реагирует на мышь)
- 🎆 Плавающие частицы и параллакс-эффекты  
- 📡 Статус серверов с кнопкой Ping и шкалой из 4 делений
- 🍎 Apple-стиль дизайна
- 📱 Полностью адаптивный дизайн

### Админ-панель (`/stats`)
- 🔐 Защита паролем
- 📊 Статистика посещений (всего, уникальных, сегодня)
- 📈 График посещений за 7 дней
- 👆 Счётчики кликов по кнопкам
- 🖥 Управление серверами (добавление/удаление)
- 🗑 Сброс статистики

### Шкала пинга
| Уровень | Делений | Цвет | Задержка |
|---------|---------|------|----------|
| Отлично | 4 | 🟢 Ярко-зелёный | < 50ms |
| Хорошо | 3 | 🟢 Зелёный | < 100ms |
| Средне | 2 | 🟡 Жёлтый | < 200ms |
| Плохо | 1 | 🔴 Красный | ≥ 200ms |
| Offline | 0 | ⬛ Контур | — |

---

## 🚀 Быстрая установка

### Вариант 1: Автоматический деплой (рекомендуется)

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/fastcon-website.git
cd fastcon-website

# 2. Запустите деплой
chmod +x deploy.sh
sudo ./deploy.sh
```

### Вариант 2: Из архива

```bash
# 1. Скопируйте архив на сервер
scp fastcon-website.tar.gz root@YOUR_SERVER:/tmp/

# 2. На сервере распакуйте и запустите
cd /tmp
tar -xzf fastcon-website.tar.gz
cd fastcon-website
chmod +x deploy.sh
sudo ./deploy.sh
```

---

## 📋 Пошаговая ручная установка

### Шаг 1: Подготовка сервера (Ubuntu 24)

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка необходимых пакетов
sudo apt install -y curl git nginx certbot python3-certbot-nginx

# Установка Docker
curl -fsSL https://get.docker.com | sudo sh
sudo systemctl enable docker
sudo systemctl start docker

# Установка Docker Compose
sudo apt install -y docker-compose-plugin
```

### Шаг 2: Настройка DNS

В панели управления доменом добавьте A-запись:

| Тип | Имя | Значение |
|-----|-----|----------|
| A | fastcon | IP_вашего_сервера |

Подождите 5-10 минут для распространения DNS.

Проверка:
```bash
dig fastcon.harknmav.fun
```

### Шаг 3: Клонирование проекта

```bash
# Создайте директорию
sudo mkdir -p /opt/fastcon
cd /opt/fastcon

# Клонируйте репозиторий (или скопируйте файлы)
git clone https://github.com/YOUR_USERNAME/fastcon-website.git .

# Или если из архива:
# tar -xzf /tmp/fastcon-website.tar.gz --strip-components=1
```

### Шаг 4: Настройка окружения

```bash
# Создайте .env файл
cp .env.example .env

# При необходимости измените пароль
nano .env
```

### Шаг 5: Получение SSL сертификата

```bash
# Остановите nginx если запущен
sudo systemctl stop nginx

# Получите сертификат
sudo certbot certonly --standalone -d fastcon.harknmav.fun

# Введите email и согласитесь с условиями
```

### Шаг 6: Настройка Nginx

```bash
# Скопируйте конфиг
sudo cp nginx/fastcon.conf /etc/nginx/sites-available/fastcon
sudo ln -sf /etc/nginx/sites-available/fastcon /etc/nginx/sites-enabled/

# Удалите дефолтный сайт (опционально)
sudo rm -f /etc/nginx/sites-enabled/default

# Проверьте конфиг
sudo nginx -t

# Запустите nginx
sudo systemctl enable nginx
sudo systemctl start nginx
```

### Шаг 7: Сборка и запуск приложения

```bash
cd /opt/fastcon

# Сборка Docker образа
sudo docker compose build

# Запуск в фоне
sudo docker compose up -d

# Проверка статуса
sudo docker compose ps

# Просмотр логов
sudo docker compose logs -f
```

### Шаг 8: Проверка

```bash
# Проверить что сайт работает
curl -I https://fastcon.harknmav.fun

# Должно вернуть HTTP/2 200
```

---

## 📁 Структура проекта

```
fastcon-website/
├── public/
│   ├── index.html      # Главная страница
│   └── stats.html      # Админ-панель
├── src/
│   └── server.js       # Node.js сервер + API
├── nginx/
│   └── fastcon.conf    # Конфиг nginx
├── Dockerfile          # Docker образ
├── docker-compose.yml  # Docker Compose конфиг
├── package.json        # Зависимости
├── .env.example        # Пример переменных окружения
├── deploy.sh           # Скрипт автодеплоя
└── README.md           # Документация
```

---

## 🔧 Управление

### Команды Docker

```bash
cd /opt/fastcon

# Просмотр логов
sudo docker compose logs -f

# Перезапуск
sudo docker compose restart

# Остановка
sudo docker compose down

# Обновление (после git pull)
sudo docker compose build
sudo docker compose up -d
```

### Обновление сайта

```bash
cd /opt/fastcon

# Получить изменения
git pull origin main

# Пересобрать и перезапустить
sudo docker compose build
sudo docker compose up -d
```

### Резервное копирование данных

```bash
# Данные хранятся в Docker volume
sudo docker cp fastcon-app:/data/stats.json ./backup-stats.json
```

### Восстановление данных

```bash
sudo docker cp ./backup-stats.json fastcon-app:/data/stats.json
sudo docker compose restart
```

---

## 🔐 Безопасность

### Изменение пароля админки

1. Отредактируйте `.env`:
```bash
nano /opt/fastcon/.env
# Измените ADMIN_PASSWORD=ваш_новый_пароль
```

2. Перезапустите:
```bash
sudo docker compose restart
```

### Автообновление SSL сертификата

Certbot автоматически настраивает cron для обновления. Проверьте:
```bash
sudo certbot renew --dry-run
```

---

## 🛠 Troubleshooting

### Сайт не открывается

```bash
# Проверьте DNS
dig fastcon.harknmav.fun

# Проверьте статус контейнера
sudo docker compose ps

# Проверьте логи
sudo docker compose logs

# Проверьте nginx
sudo nginx -t
sudo systemctl status nginx
```

### 502 Bad Gateway

```bash
# Проверьте что приложение запущено
sudo docker compose ps

# Перезапустите
sudo docker compose restart

# Проверьте порт
curl http://127.0.0.1:3000
```

### Сертификат не выдаётся

```bash
# Убедитесь что порты открыты
sudo ufw allow 80
sudo ufw allow 443

# Проверьте что nginx остановлен перед получением сертификата
sudo systemctl stop nginx
sudo certbot certonly --standalone -d fastcon.harknmav.fun
sudo systemctl start nginx
```

### Ping не работает

Ping требует ICMP пакеты. Убедитесь что:
1. Контейнер имеет права на raw sockets (уже настроено)
2. Целевой хост отвечает на ping

---

## 📊 API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/visit` | Трекинг посещения |
| POST | `/api/click` | Трекинг клика |
| GET | `/api/ping/:host` | Ping сервера |
| GET | `/api/admin/stats` | Статистика (требует пароль) |
| POST | `/api/admin/reset` | Сброс статистики |
| GET | `/api/admin/servers` | Список серверов |
| POST | `/api/admin/servers` | Добавить сервер |
| DELETE | `/api/admin/servers/:id` | Удалить сервер |

Для админских endpoints передавайте пароль в заголовке `X-Admin-Password`.

---

## 📜 Лицензия

MIT License

---

## 🤝 Контакты

- Telegram: [@obhod_support](https://t.me/obhod_support)
- Канал: [@fastconnews](https://t.me/fastconnews)
